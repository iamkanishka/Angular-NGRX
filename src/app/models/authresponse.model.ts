export interface AuthResponseData {
    displayName: ""
    email: string
    expiresIn: string
    idToken: string
    kind: string
    localId: string
    refreshToken: string
    registered?: true
}